'use client'

import { WalletActionButton } from '@/components/shared/wallet-action-button'

export const WalletChip = () => {
  return <WalletActionButton labelDisconnected="Connect" variant="cc-outline" />
}
